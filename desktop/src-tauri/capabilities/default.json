{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "main window: core apis plus external links",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
